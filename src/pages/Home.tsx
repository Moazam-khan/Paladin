import {Col, Row} from 'antd';
import {Text} from '../components';

type Props = {};

const Home = (props: Props) => {
  return (
    <Row>
      <Col>
        <Text>PALADIN boiler</Text>
      </Col>
    </Row>
  );
};

export default Home;
